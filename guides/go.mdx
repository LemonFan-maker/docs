---
title: "Go 接入指南"
description: "使用 Go 接入 OrionisLi API"
icon: "golang"
---

## OpenAI SDK

### 安装

```bash
go get github.com/sashabaranov/go-openai
```

### 基本使用

```go
package main

import (
	"context"
	"fmt"
	"log"

	openai "github.com/sashabaranov/go-openai"
)

func main() {
	config := openai.DefaultConfig("sk-你的密钥")
	config.BaseURL = "https://sub.orionisli.cn/v1"

	client := openai.NewClientWithConfig(config)

	resp, err := client.CreateChatCompletion(
		context.Background(),
		openai.ChatCompletionRequest{
			Model: "gpt-5.4",
			Messages: []openai.ChatCompletionMessage{
				{Role: openai.ChatMessageRoleUser, Content: "你好"},
			},
		},
	)
	if err != nil {
		log.Fatalf("请求失败: %v", err)
	}

	fmt.Println(resp.Choices[0].Message.Content)
}
```

### 多轮对话

```go
package main

import (
	"bufio"
	"context"
	"fmt"
	"log"
	"os"

	openai "github.com/sashabaranov/go-openai"
)

func main() {
	config := openai.DefaultConfig("sk-你的密钥")
	config.BaseURL = "https://sub.orionisli.cn/v1"
	client := openai.NewClientWithConfig(config)

	messages := []openai.ChatCompletionMessage{
		{Role: openai.ChatMessageRoleSystem, Content: "你是一个有帮助的助手。"},
	}

	scanner := bufio.NewScanner(os.Stdin)
	for {
		fmt.Print("你: ")
		if !scanner.Scan() {
			break
		}
		input := scanner.Text()
		if input == "exit" || input == "quit" {
			break
		}

		messages = append(messages, openai.ChatCompletionMessage{
			Role:    openai.ChatMessageRoleUser,
			Content: input,
		})

		resp, err := client.CreateChatCompletion(
			context.Background(),
			openai.ChatCompletionRequest{
				Model:    "gpt-5.4",
				Messages: messages,
			},
		)
		if err != nil {
			log.Printf("请求失败: %v", err)
			continue
		}

		reply := resp.Choices[0].Message.Content
		messages = append(messages, openai.ChatCompletionMessage{
			Role:    openai.ChatMessageRoleAssistant,
			Content: reply,
		})
		fmt.Printf("助手: %s\n", reply)
	}
}
```

### 使用环境变量

```go
package main

import (
	"context"
	"fmt"
	"log"
	"os"

	openai "github.com/sashabaranov/go-openai"
)

func main() {
	apiKey := os.Getenv("OPENAI_API_KEY")
	if apiKey == "" {
		log.Fatal("请设置 OPENAI_API_KEY 环境变量")
	}

	config := openai.DefaultConfig(apiKey)
	config.BaseURL = "https://sub.orionisli.cn/v1"

	client := openai.NewClientWithConfig(config)

	resp, err := client.CreateChatCompletion(
		context.Background(),
		openai.ChatCompletionRequest{
			Model: "gpt-5.4",
			Messages: []openai.ChatCompletionMessage{
				{Role: openai.ChatMessageRoleUser, Content: "你好"},
			},
		},
	)
	if err != nil {
		log.Fatalf("请求失败: %v", err)
	}

	fmt.Println(resp.Choices[0].Message.Content)
}
```

### 流式输出

```go
stream, err := client.CreateChatCompletionStream(
	context.Background(),
	openai.ChatCompletionRequest{
		Model: "gpt-5.4",
		Messages: []openai.ChatCompletionMessage{
			{Role: openai.ChatMessageRoleUser, Content: "讲一个故事"},
		},
		Stream: true,
	},
)
if err != nil {
	log.Fatalf("请求失败: %v", err)
}
defer stream.Close()

for {
	response, err := stream.Recv()
	if err != nil {
		break
	}
	fmt.Print(response.Choices[0].Delta.Content)
}
fmt.Println()
```

### 错误处理

```go
resp, err := client.CreateChatCompletion(
	context.Background(),
	openai.ChatCompletionRequest{
		Model: "gpt-5.4",
		Messages: []openai.ChatCompletionMessage{
			{Role: openai.ChatMessageRoleUser, Content: "你好"},
		},
	},
)
if err != nil {
	// 检查错误类型
	apiErr := &openai.APIError{}
	if errors.As(err, &apiErr) {
		switch apiErr.HTTPStatusCode {
		case 401:
			fmt.Println("API Key 无效")
		case 429:
			fmt.Println("请求过于频繁")
		case 500:
			fmt.Println("服务器错误")
		default:
			fmt.Printf("API 错误 [%d]: %s\n", apiErr.HTTPStatusCode, apiErr.Message)
		}
	} else {
		fmt.Printf("请求失败: %v\n", err)
	}
	return
}
```

## 直接使用 HTTP

不依赖第三方 SDK，使用标准库调用。

```go
package main

import (
	"bytes"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
	"os"
)

func main() {
	body, _ := json.Marshal(map[string]interface{}{
		"model": "gpt-5.4",
		"messages": []map[string]string{
			{"role": "user", "content": "你好"},
		},
	})

	req, _ := http.NewRequest("POST",
		"https://sub.orionisli.cn/v1/chat/completions",
		bytes.NewBuffer(body),
	)
	req.Header.Set("Authorization", "Bearer sk-你的密钥")
	req.Header.Set("Content-Type", "application/json")

	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		fmt.Printf("请求失败: %v\n", err)
		return
	}
	defer resp.Body.Close()

	data, _ := io.ReadAll(resp.Body)
	fmt.Println(string(data))
}
```

## 常见问题

<AccordionGroup>
  <Accordion title="模型名称不匹配">
    SDK 中的模型常量可能不包含最新模型。直接使用字符串即可：
    ```go
    Model: "gpt-5.4"
    ```
  </Accordion>
  <Accordion title="连接超时">
    检查网络是否可以访问 `sub.orionisli.cn`。如有代理需求，请配置 `HTTP_PROXY` 环境变量。
  </Accordion>
</AccordionGroup>
