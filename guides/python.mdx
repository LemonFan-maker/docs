---
title: "Python 接入指南"
description: "使用 Python 接入 OrionisLi API，支持 OpenAI 和 Anthropic SDK"
icon: "python"
---

## OpenAI SDK

适用于 GPT 系列模型。

### 安装

```bash
pip install openai
```

### 基本使用

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn/v1"
)

response = client.chat.completions.create(
    model="gpt-5.4",
    messages=[
        {"role": "system", "content": "你是一个有帮助的助手。"},
        {"role": "user", "content": "请用 Python 写一个快速排序算法"}
    ]
)

print(response.choices[0].message.content)
```

### 流式输出

```python
stream = client.chat.completions.create(
    model="gpt-5.4",
    messages=[{"role": "user", "content": "讲一个故事"}],
    stream=True
)

for chunk in stream:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="")
```

### 多轮对话

维护对话上下文，实现多轮对话：

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn/v1"
)

messages = [
    {"role": "system", "content": "你是一个有帮助的助手。"}
]

while True:
    user_input = input("你: ")
    if user_input.lower() in ["exit", "quit", "q"]:
        break

    messages.append({"role": "user", "content": user_input})

    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=messages
    )

    assistant_msg = response.choices[0].message.content
    messages.append({"role": "assistant", "content": assistant_msg})
    print(f"助手: {assistant_msg}")
```

### 函数调用（Function Calling）

```python
from openai import OpenAI
import json

client = OpenAI(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn/v1"
)

tools = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "获取指定城市的天气信息",
            "parameters": {
                "type": "object",
                "properties": {
                    "city": {
                        "type": "string",
                        "description": "城市名称，如 '北京'"
                    }
                },
                "required": ["city"]
            }
        }
    }
]

response = client.chat.completions.create(
    model="gpt-5.4",
    messages=[{"role": "user", "content": "北京今天天气怎么样？"}],
    tools=tools,
    tool_choice="auto"
)

message = response.choices[0].message

if message.tool_calls:
    tool_call = message.tool_calls[0]
    func_name = tool_call.function.name
    args = json.loads(tool_call.function.arguments)
    print(f"调用函数: {func_name}({args})")

    # 这里执行实际的函数调用，然后把结果返回给模型
    # result = get_weather(**args)
```

### 图像输入

使用支持视觉的模型（如 gpt-4o 系列）分析图片：

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn/v1"
)

response = client.chat.completions.create(
    model="gpt-5.4",
    messages=[
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "这张图片里有什么？"},
                {
                    "type": "image_url",
                    "image_url": {
                        "url": "https://example.com/image.jpg"
                    }
                }
            ]
        }
    ]
)

print(response.choices[0].message.content)
```

### 使用环境变量（推荐）

```bash
export OPENAI_API_KEY=sk-你的密钥
export OPENAI_BASE_URL=https://sub.orionisli.cn/v1
```

```python
from openai import OpenAI

client = OpenAI()  # 自动读取环境变量
```

### 使用 python-dotenv 管理密钥

```bash
pip install python-dotenv
```

创建 `.env` 文件：

```
OPENAI_API_KEY=sk-你的密钥
OPENAI_BASE_URL=https://sub.orionisli.cn/v1
```

```python
from dotenv import load_dotenv
from openai import OpenAI

load_dotenv()
client = OpenAI()  # 自动读取 .env 中的环境变量
```

<Warning>
  将 `.env` 添加到 `.gitignore`，避免密钥泄露。
</Warning>

## Anthropic SDK

适用于 Claude 系列模型。

### 安装

```bash
pip install anthropic
```

### 基本使用

```python
import anthropic

client = anthropic.Anthropic(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn"
)

message = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "请用 Python 写一个快速排序算法"}
    ]
)

print(message.content[0].text)
```

### 带系统提示词

```python
message = client.messages.create(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    system="你是一个资深 Python 开发者，回答要简洁专业。",
    messages=[
        {"role": "user", "content": "Python 中如何处理并发？"}
    ]
)
```

### 流式输出

```python
with client.messages.stream(
    model="claude-sonnet-4-6",
    max_tokens=1024,
    messages=[{"role": "user", "content": "讲一个故事"}]
) as stream:
    for text in stream.text_stream:
        print(text, end="")
```

### 错误处理

```python
from openai import OpenAI, APIError, AuthenticationError, RateLimitError

client = OpenAI(
    api_key="sk-你的密钥",
    base_url="https://sub.orionisli.cn/v1"
)

try:
    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=[{"role": "user", "content": "你好"}]
    )
    print(response.choices[0].message.content)
except AuthenticationError:
    print("API Key 无效，请检查密钥")
except RateLimitError:
    print("请求过于频繁，请稍后重试")
except APIError as e:
    print(f"API 错误: {e}")
```

## 直接使用 requests

不依赖 SDK，直接调用 HTTP API。

```python
import requests

response = requests.post(
    "https://sub.orionisli.cn/v1/chat/completions",
    headers={
        "Authorization": "Bearer sk-你的密钥",
        "Content-Type": "application/json"
    },
    json={
        "model": "gpt-5.4",
        "messages": [{"role": "user", "content": "你好"}]
    }
)

data = response.json()
print(data["choices"][0]["message"]["content"])
```

### 使用 httpx 异步调用

```bash
pip install httpx
```

```python
import httpx
import asyncio

async def chat(message: str) -> str:
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "https://sub.orionisli.cn/v1/chat/completions",
            headers={
                "Authorization": "Bearer sk-你的密钥",
                "Content-Type": "application/json"
            },
            json={
                "model": "gpt-5.4",
                "messages": [{"role": "user", "content": message}]
            },
            timeout=60.0
        )
        data = response.json()
        return data["choices"][0]["message"]["content"]

# 使用
result = asyncio.run(chat("你好"))
print(result)
```

## 常见问题

<AccordionGroup>
  <Accordion title="ModuleNotFoundError: No module named 'openai'">
    运行 `pip install openai` 安装 SDK。如果使用虚拟环境，请确认已激活。
  </Accordion>
  <Accordion title="AuthenticationError">
    检查 API Key 是否正确，是否以 `sk-` 开头。确认 `base_url` 设置为 `https://sub.orionisli.cn/v1`。
  </Accordion>
  <Accordion title="RateLimitError">
    请求过于频繁，请稍后重试。如需更高并发，请联系管理员。
  </Accordion>
  <Accordion title="ConnectionError">
    检查网络是否可以访问 `sub.orionisli.cn`。如果使用代理，配置 `HTTPS_PROXY` 环境变量。
  </Accordion>
</AccordionGroup>
